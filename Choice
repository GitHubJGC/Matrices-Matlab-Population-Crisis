Round = input('What percentage do you wish to spend on Healthcare? The rest will be spent on Education. The amounts will be rounded to the nearest 10...    ');
if Round ~=0:100
    display 'must be a number from 0 to 100'
    
else
    HealthP=roundn(Round,1);
    EducP = 100-HealthP;
    HMRS = HealthP + 1;
    HMRE = HealthP + 10;
    EMR = (EducP/10)+1;
   
    EC = EducM(EMR,:);
    HC = HealthM(HMRS:HMRE,:);
end
