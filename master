%%Tax calculation to have money to begin with

%% ini
LeslieMatrix;
EducHealthM;
Choice;
Tax2;
EducHealth;

%% loop

y = length(p);
X = zeros(y,n+1);
X(:,1)= p;
%c=0;

for j = 1:n
    %c=c+1;
TaxRev0 = Tax(t,WP);
TaxRev1 = Pension(p,TaxRev0);
SppCheck;
%pt = PopChange(p,HC,E,j,X);

WP=X(:,j)*E;
X(:,j+1) = HC*X(:,j);

X1 = X;

hold on
plot(j,SPP,'r-*');
end
%%
